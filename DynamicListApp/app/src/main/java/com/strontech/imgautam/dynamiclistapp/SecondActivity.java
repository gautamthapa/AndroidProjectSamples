package com.strontech.imgautam.dynamiclistapp;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.ArrayAdapter;
import android.widget.ListView;

import java.util.ArrayList;

public class SecondActivity extends AppCompatActivity {



    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_second);
        /*listView = findViewById(R.id.listView1);


        Bundle b = getIntent().getExtras();

        //assert b != null;
        String n = b.getString("sName");
        String r = b.getString("sRoll");
        String c = b.getString("sClas");

        ArrayAdapter<String> adapter = new ArrayAdapter<String>(this, android.R.layout.simple_list_item_1, al);
        listView.setAdapter(adapter);
       al.add(n);
      al.add(r);
       al.add(c);
*/


    }
}
